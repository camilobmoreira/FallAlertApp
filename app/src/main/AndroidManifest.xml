<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="br.com.aimcol.fallalertapp">

    <uses-feature
        android:name="android.hardware.sensor.accelerometer"
        android:required="true" />
    <uses-feature
        android:name="android.hardware.sensor.gyroscope"
        android:required="false" />

    <uses-permission android:name="android.permission.SEND_SMS" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".service.UserService"
            android:exported="false" />
        <service
            android:name=".service.FallDetectionService"
            android:exported="false" />
        <service
            android:name=".service.FallNotificationService"
            android:exported="false" />
        <service android:name=".service.FallHistoryService" />

        <activity android:name=".activity.MainActivity" />
        <activity android:name=".activity.NewCaregiverActivity" />
        <activity android:name=".activity.NewElderlyActivity" />
        <activity android:name=".activity.FallNotificationActivity" />
        <activity
            android:name=".activity.ConfigurationsActivity"
            android:label="@string/title_activity_configurations"></activity>
    </application>

</manifest>